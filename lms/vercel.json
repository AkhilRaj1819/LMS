{
  "buildCommand": "cd lms && npm install && next build",
  "outputDirectory": "lms/.next",
  "framework": "nextjs",
  "installCommand": "cd lms && npm install",
  "functions": {
    "app/api/**": {
      "maxDuration": 30
    }
  }
}